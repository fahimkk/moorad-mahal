import React from 'react'

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Mahal | Donations",
  description: "View all donations and contributions made to Moorad Mahal.",
};

export default function DonationsPage() {
    return (
      <main>
        <h1>Donations</h1>
      </main>
    );
  }
